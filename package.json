{
    "name": "laboratorio-gh",
    "version": "0.1.0",
    "scripts": {
        "start": "node index.js ",
        "heroku-postbuild": "npm install --prefix API && npm install --prefix laboratorio"
    }
  }